# bug-quize

coverity-workflow.yml has already configured.
1. confirm up and running self-hosted runner on AWS
2. check the IP address of the runner and configure GitHub Environment
3. enable coverity-workflow.yml
   
